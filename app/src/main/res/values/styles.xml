<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo">
        <item name="android:windowFullscreen">true</item>
        <item name="android:actionBarSize">50dp</item>
        <item name="android:actionBarStyle">@style/myActionBarStyle</item>
        <item name="android:dialogTheme">@style/StatusDialog</item>
    </style>
    <style name="datePickerDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <style name="myActionBarStyle" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:logo">@drawable/icon</item>
    </style>
    <style name="verLine_header">
        <item name="android:layout_width">0.5dp</item>
        <item name="android:layout_height">@dimen/verLineHeight_header</item>
        <item name="android:background">@color/colorLine</item>
    </style>
    <style name="verLine_item">
        <item name="android:layout_width">0.5dp</item>
        <item name="android:layout_height">@dimen/verLineHeight_item</item>
        <item name="android:background">@color/colorLine</item>
    </style>
    <style name="listHeaderStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/verLineHeight_header</item>
        <item name="android:background">@color/colorListHeader</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="listItemStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/verLineHeight_item</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="StatusDialog" parent="@android:style/Theme.Holo.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
